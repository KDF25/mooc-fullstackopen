sequenceDiagram
    participant browser
    participant server

    Note over browser: The user types a note and clicks the "Save" button
    Note over browser: The JS code (spa.js) intercepts the form submission,<br/>adds the new note to the local list, and<br/>immediately updates the page using DOM manipulation

    browser->>server: POST https://helsinki.fi
    activate server
    Note over server: The server receives the JSON data and appends the new note to the array
    server-->>browser: HTTP 201 Created
    deactivate server
    
    Note over browser: No page reload or further requests are needed
